import { Container } from "../styles/inactive.styled";
function Inactive(props) {
    return (
        <Container>
            <button>Inativo</button>
        </Container>
    );
}

export default Inactive;